MapPage {
    background-color: $bg_dark;
}

/* --- TimeLineControls Section --- */

TimelineControls {
    background-color: rgba(30, 30, 30, 240);
}

/* --- Bottom Dock Styles --- */

#bottom_dock {
    background-color: rgba(40, 40, 40, 230);
    border: 1px solid rgba(255, 255, 255, 0.1);
}

/* Bottom dock buttons are now in the left sidebar */
#bottom_dock_button {
    background-color: transparent;
    color: #ccc;
    border: none;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
    text-align: center;
    font-size: 11px;
    font-weight: 500;
    padding: 8px 4px;
}

#bottom_dock_button:hover {
    background-color: rgba(255, 255, 255, 0.05);
    color: #fff;
}

#bottom_dock_button:checked {
    background-color: rgba(33, 150, 243, 0.3);
    color: #2196F3;
    border-left: 3px solid #2196F3;
}

/* --- Sidebar Styles --- */

#sidebar {
    background-color: transparent;
}

#sidebar_button_strip {
    background-color: rgba(30, 30, 30, 200);
    border-right: 1px solid rgba(255, 255, 255, 0.1);
}

#sidebar_dock_container {
    background-color: rgba(40, 40, 40, 230);
    border: 1px solid rgba(255, 255, 255, 0.1);
}

#sidebar_dock_button {
    background-color: transparent;
    color: #ccc;
    border: none;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    text-align: center;
    font-size: 11px;
    font-weight: 500;
    padding: 8px 4px;
}

#sidebar_dock_button:hover {
    background-color: rgba(255, 255, 255, 0.05);
    color: #fff;
}

#sidebar_dock_button:checked {
    background-color: rgba(33, 150, 243, 0.3);
    color: #2196F3;
    border-left: 3px solid #2196F3;
}

/* --- Dock Styles --- */

#game_info_dock,
#province_info_dock,
#army_info_dock,
#events_dock,
#city_list_dock,
#army_list_dock {
    background-color: rgba(40, 40, 40, 230);
}

#dock_title {
    color: #fff;
    font-size: 16px;
    font-weight: bold;
}

#dock_separator {
    background-color: rgba(255, 255, 255, 0.1);
    border: none;
}

#dock_label {
    color: #888;
    font-size: 12px;
}

#dock_value {
    color: #fff;
    font-size: 12px;
    font-weight: 500;
}

/* ScrollArea styling for docks */
QScrollArea {
    background-color: transparent;
    border: none;
}

QScrollArea > QWidget > QWidget {
    background-color: transparent;
}

QScrollBar:vertical {
    background-color: rgba(255, 255, 255, 0.05);
    width: 8px;
    border-radius: 4px;
}

QScrollBar::handle:vertical {
    background-color: rgba(255, 255, 255, 0.2);
    border-radius: 4px;
    min-height: 20px;
}

QScrollBar::handle:vertical:hover {
    background-color: rgba(255, 255, 255, 0.3);
}

QScrollBar::add-line:vertical,
QScrollBar::sub-line:vertical {
    height: 0px;
}

QScrollBar::add-page:vertical,
QScrollBar::sub-page:vertical {
    background: none;
}
